# Bundle patch layer for @agfs/dsh-agfs. Mounting this package as a
# profile layer (published users: `dsh plugin --profile <name> add
# @agfs/dsh-agfs`) inserts the file-browser row; per-layer resolution
# anchors on the bundle package directory.
- insert:
    - id: dsh-agfs
      name: '@agfs/dsh-agfs'
