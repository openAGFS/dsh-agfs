# Bundle patch layer for @open-agfs/dsh-agfs. Mounting this package as a
# profile layer (published users: `dsh plugin --profile <name> add
# @open-agfs/dsh-agfs`) inserts the file-browser row; per-layer resolution
# anchors on the bundle package directory.
- insert:
    - id: dsh-agfs
      name: '@open-agfs/dsh-agfs'
